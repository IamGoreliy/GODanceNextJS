import {createSlice} from '@reduxjs/toolkit';

const editUserSlice = createSlice({
  name: 'editUser',
  initialState: {

  },
  extraReducers: builder =>
    builder
      .addCase()
      .addCase()
      .addCase()
})